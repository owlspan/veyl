// One real mistake must produce one error, not five. Everything
// downstream of `broken` is unknown, and unknown stays quiet.
let broken = "text" + 1
let a = broken + broken
let b = a * 2
let c = sqrt(b)
print(c)
