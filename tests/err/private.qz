import "mod/geometry.qz"

// geometry.qz declares these without pub, so they are private to it.
print(secret())
print(fudge)

// A private struct cannot be named either.
import "mod/hidden.qz"
let h = Hidden{n: 1}
print(usesHidden())
