// Four independent mistakes should give four errors, not fourteen.
// Each one names what was left open and where it was opened, which is
// the part you cannot work out from "expected ')'".

fn area(w: int, h: int -> int {
    return w * h
}

let xs = [
    1,
    2,
    3
print(xs)

let m = {
    "a": 1,
    "b": 2

let p = (1 + 2
print(p)
