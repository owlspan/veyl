// Imported by sample.vl, so the sample is a real program rather than a
// snippet that only looks like one.

pub const GREETING = "hello"

pub fn shout(text: str) -> str {
    return "{upper(text)}!"
}
